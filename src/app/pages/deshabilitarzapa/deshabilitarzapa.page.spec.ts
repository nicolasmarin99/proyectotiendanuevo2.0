import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeshabilitarzapaPage } from './deshabilitarzapa.page';

describe('DeshabilitarzapaPage', () => {
  let component: DeshabilitarzapaPage;
  let fixture: ComponentFixture<DeshabilitarzapaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DeshabilitarzapaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
